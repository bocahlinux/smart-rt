# Used by docker-compose.yml for the PostgreSQL service.
# Backend & frontend app config lives in backend/.env and frontend/.env (see their .env.example).
POSTGRES_DB=smartrt
POSTGRES_USER=smartrt
POSTGRES_PASSWORD=smartrt
